---
phase: 05-pi-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - io.github.meintechblog.mqtt-master.sdPlugin/ui/mqtt-action.css
  - io.github.meintechblog.mqtt-master.sdPlugin/ui/mqtt-action.html
autonomous: false
requirements: [PI-01, PI-02, PI-03]

must_haves:
  truths:
    - "All PI text is clearly readable -- white (#ffffff) on dark gray (#2D2D2D)"
    - "5 sections (Broker, Subscribe, Publish, Toggle, Long Press) are visually separated with styled header bars"
    - "Broker section is open by default, all others collapsed"
    - "Broker, Subscribe, and Publish sections have Advanced toggles hiding less-used fields"
    - "Toggle and Long Press sections show all fields (no advanced toggle)"
    - "All setting= attributes unchanged -- settings auto-binding still works"
  artifacts:
    - path: "io.github.meintechblog.mqtt-master.sdPlugin/ui/mqtt-action.css"
      provides: "Custom dark theme overrides and accordion styling"
      contains: "--font-color: #ffffff"
    - path: "io.github.meintechblog.mqtt-master.sdPlugin/ui/mqtt-action.html"
      provides: "Restructured PI with details/summary accordion and advanced toggles"
      contains: "<details open>"
  key_links:
    - from: "mqtt-action.html"
      to: "mqtt-action.css"
      via: "link rel=stylesheet in head"
      pattern: "mqtt-action\\.css"
    - from: "mqtt-action.html"
      to: "sdpi-components.js"
      via: "script tag in head"
      pattern: "sdpi-components\\.js"
---

<objective>
Restyle the Property Inspector with a custom dark theme CSS file and restructure the HTML into collapsible accordion sections with advanced field toggles.

Purpose: Fix the "Augenkrebs" readability problem (gray text on dark gray) and organize the growing number of fields into a clean, navigable UI that matches Stream Deck's native dark aesthetic.
Output: Two files -- mqtt-action.css (new) and mqtt-action.html (restructured)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-pi-redesign/05-CONTEXT.md
@.planning/phases/05-pi-redesign/05-RESEARCH.md

@io.github.meintechblog.mqtt-master.sdPlugin/ui/mqtt-action.html
@src/types/settings.ts

<interfaces>
<!-- Current PI HTML structure (80 lines, flat layout with h2 + hr separators) -->
<!-- All setting= attribute values from settings.ts that MUST be preserved: -->
<!--   brokerHost, brokerPort, brokerUsername, brokerPassword, brokerTls -->
<!--   subscribeTopic, jsonPath, displayTemplate -->
<!--   publishTopic, publishPayload, qos, retain -->
<!--   onPayload, offPayload, onValue, offValue -->
<!--   longPressTopic, longPressPayload -->

<!-- sdpi-components v3 CSS custom properties (from RESEARCH.md): -->
<!--   --font-color: #969696 (THE PROBLEM - override to #ffffff) -->
<!--   --input-font-color: #d8d8d8 (override to #ffffff) -->
<!--   --window-bg-color: #2d2d2d (keep) -->
<!--   --input-bg-color: #3d3d3d (keep) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create mqtt-action.css with dark theme overrides and accordion styling</name>
  <files>io.github.meintechblog.mqtt-master.sdPlugin/ui/mqtt-action.css</files>
  <read_first>
    - .planning/phases/05-pi-redesign/05-RESEARCH.md (Pattern 1-4 CSS examples)
    - io.github.meintechblog.mqtt-master.sdPlugin/ui/mqtt-action.html (current structure)
  </read_first>
  <action>
Create NEW file `io.github.meintechblog.mqtt-master.sdPlugin/ui/mqtt-action.css` with these sections:

**1. Theme Overrides (per D-32, D-33 -- PI-01):**
```css
:root {
  --font-color: #ffffff;
  --input-font-color: #ffffff;
  /* --window-bg-color and --input-bg-color already correct, no override needed */
}
body { margin: 0; padding: 4px; }
```

**2. Section Accordion Styling (per D-34, D-36 -- PI-02):**
- `details` -- margin-bottom: 4px
- `details > summary` -- styled as clickable bar: background #383838, color #ffffff, padding 8px 12px, cursor pointer, border-radius 4px, font-weight 600, font-size 11pt, list-style none, display flex, align-items center, gap 8px, user-select none
- `details > summary::-webkit-details-marker` -- display none (remove default marker)
- `details > summary::before` -- content "\25B6" (right triangle), font-size 8px, transition transform 0.2s ease
- `details[open] > summary::before` -- transform rotate(90deg) (points down when open)
- `details[open] > summary` -- border-radius 4px 4px 0 0 (flat bottom when open)

**3. Section Content:**
- `.section-content` -- padding: 8px 4px 4px 4px

**4. Advanced Toggle Styling (per D-38 -- PI-03):**
- `details.advanced` -- margin-top: 4px
- `details.advanced > summary.advanced-toggle` -- background transparent, color #0078D7 (blue accent per D-32), font-size 8pt, font-weight normal, padding 4px 0, list-style none, cursor pointer, user-select none
- `details.advanced > summary.advanced-toggle::-webkit-details-marker` -- display none
- `details.advanced > summary.advanced-toggle::before` -- content "+" , font-size 10px, margin-right 4px
- `details.advanced[open] > summary.advanced-toggle::before` -- content "\2212" (minus sign)

**5. Focus/accent states:**
- `details > summary:hover` -- background #404040
- `details > summary:focus-visible` -- outline: 2px solid #0078D7, outline-offset: -2px
  </action>
  <verify>
    <automated>test -f "io.github.meintechblog.mqtt-master.sdPlugin/ui/mqtt-action.css" && grep -q "\-\-font-color: #ffffff" "io.github.meintechblog.mqtt-master.sdPlugin/ui/mqtt-action.css" && grep -q "section-content" "io.github.meintechblog.mqtt-master.sdPlugin/ui/mqtt-action.css" && grep -q "advanced-toggle" "io.github.meintechblog.mqtt-master.sdPlugin/ui/mqtt-action.css" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at io.github.meintechblog.mqtt-master.sdPlugin/ui/mqtt-action.css
    - Contains --font-color: #ffffff override on :root
    - Contains --input-font-color: #ffffff override on :root
    - Contains details > summary styling with background #383838
    - Contains .advanced-toggle styling with color #0078D7
    - Contains ::before pseudo-element for expand/collapse indicator
    - Contains ::-webkit-details-marker { display: none }
  </acceptance_criteria>
  <done>CSS file created with dark theme overrides, accordion header bars, and advanced toggle styles</done>
</task>

<task type="auto">
  <name>Task 2: Restructure mqtt-action.html with accordion sections and advanced toggles</name>
  <files>io.github.meintechblog.mqtt-master.sdPlugin/ui/mqtt-action.html</files>
  <read_first>
    - io.github.meintechblog.mqtt-master.sdPlugin/ui/mqtt-action.html (current flat structure)
    - .planning/phases/05-pi-redesign/05-RESEARCH.md (Pattern 2 HTML structure, Pitfall 5 about setting= preservation)
    - .planning/phases/05-pi-redesign/05-CONTEXT.md (D-34 through D-39 decisions)
    - src/types/settings.ts (all setting= attribute names to preserve)
  </read_first>
  <action>
Rewrite `io.github.meintechblog.mqtt-master.sdPlugin/ui/mqtt-action.html` with these changes:

**1. Head section:**
- Keep existing script tag for sdpi-components.js
- Add AFTER the script tag: `<link rel="stylesheet" href="mqtt-action.css" />`
  (CSS after script per Pitfall 2 in RESEARCH.md)

**2. Replace flat h2/hr structure with details/summary accordion (per D-34, D-35, D-37):**

CRITICAL: Every setting= attribute value MUST remain IDENTICAL to the current file. Do NOT rename, remove, or reorder any setting= attributes. The sdpi-item/sdpi-textfield/sdpi-select/sdpi-checkbox/sdpi-password elements stay exactly as they are -- only wrap them in new container elements.

**Section 1: Broker (open by default per D-35)**
```html
<details open>
  <summary>Broker</summary>
  <div class="section-content">
    <!-- Basic: Host, Port (same sdpi-items as current lines 9-14) -->
    <details class="advanced">
      <summary class="advanced-toggle">Advanced</summary>
      <div class="advanced-content">
        <!-- Username, Password, TLS (same sdpi-items as current lines 15-23) -->
      </div>
    </details>
  </div>
</details>
```

**Section 2: Subscribe (collapsed per D-35)**
```html
<details>
  <summary>Subscribe</summary>
  <div class="section-content">
    <!-- Basic: Topic (same sdpi-item as current line 27-29) -->
    <details class="advanced">
      <summary class="advanced-toggle">Advanced</summary>
      <div class="advanced-content">
        <!-- JSON Path, Display Template (same sdpi-items as current lines 30-35) -->
      </div>
    </details>
  </div>
</details>
```

**Section 3: Publish (collapsed per D-35)**
```html
<details>
  <summary>Publish</summary>
  <div class="section-content">
    <!-- Basic: Topic, Payload (same sdpi-items as current lines 39-44) -->
    <details class="advanced">
      <summary class="advanced-toggle">Advanced</summary>
      <div class="advanced-content">
        <!-- QoS, Retain (same sdpi-items as current lines 45-54) -->
      </div>
    </details>
  </div>
</details>
```

**Section 4: Toggle (collapsed, NO advanced toggle per D-39)**
```html
<details>
  <summary>Toggle</summary>
  <div class="section-content">
    <!-- All 4 fields visible: On Payload, Off Payload, On Value, Off Value -->
  </div>
</details>
```

**Section 5: Long Press (collapsed, NO advanced toggle per D-39)**
```html
<details>
  <summary>Long Press</summary>
  <div class="section-content">
    <!-- Both fields visible: Topic, Payload -->
  </div>
</details>
```

**3. Remove all `<h2>` headers and `<hr />` separators** -- replaced by details/summary.
  </action>
  <verify>
    <automated>grep -c "setting=" "io.github.meintechblog.mqtt-master.sdPlugin/ui/mqtt-action.html" | grep -q "16" && grep -q "mqtt-action.css" "io.github.meintechblog.mqtt-master.sdPlugin/ui/mqtt-action.html" && grep -q "<details open>" "io.github.meintechblog.mqtt-master.sdPlugin/ui/mqtt-action.html" && grep -c "<details>" "io.github.meintechblog.mqtt-master.sdPlugin/ui/mqtt-action.html" | grep -qE "^[78]$" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File contains exactly 16 setting= attributes (same count as original)
    - All original setting= values present: brokerHost, brokerPort, brokerUsername, brokerPassword, brokerTls, subscribeTopic, jsonPath, displayTemplate, publishTopic, publishPayload, qos, retain, onPayload, offPayload, onValue, offValue, longPressTopic, longPressPayload
    - Contains link to mqtt-action.css AFTER sdpi-components script tag
    - Contains exactly 1 details open (Broker section)
    - Contains 4 collapsed details without open attribute (Subscribe, Publish, Toggle, Long Press)
    - Contains 3 details class=advanced (Broker, Subscribe, Publish)
    - No h2 or hr elements remain
  </acceptance_criteria>
  <done>HTML restructured with 5 accordion sections, 3 advanced toggles, CSS linked, all settings preserved</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of PI redesign</name>
  <files>io.github.meintechblog.mqtt-master.sdPlugin/ui/mqtt-action.html, io.github.meintechblog.mqtt-master.sdPlugin/ui/mqtt-action.css</files>
  <action>
Build the plugin and present to user for visual verification. Run `npm run build` to ensure the plugin compiles with the new CSS and HTML files.
  </action>
  <verify>
    <automated>cd /Users/hulki/codex/stream-deck-master && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>User has visually confirmed: (a) white readable text on dark background, (b) 5 collapsible sections with styled headers, (c) advanced toggles work, (d) settings still save correctly</done>
</task>

</tasks>

<verification>
1. CSS file exists and contains theme overrides + accordion + advanced toggle styles
2. HTML file restructured with details/summary, CSS linked, all 16+ setting= attributes preserved
3. No h2 or hr elements remain in HTML
4. Build succeeds without errors
5. Visual confirmation from user that text is readable and sections work
</verification>

<success_criteria>
- PI text is white (#ffffff) on dark gray (#2D2D2D) background -- no low-contrast elements
- 5 sections visually separated with clickable header bars and expand/collapse indicators
- Broker section open by default, all others collapsed
- Advanced toggles work in Broker, Subscribe, and Publish sections
- All settings auto-binding via sdpi-components still functional
- Custom CSS file loaded and applied
</success_criteria>

<output>
After completion, create `.planning/phases/05-pi-redesign/05-01-SUMMARY.md`
</output>
